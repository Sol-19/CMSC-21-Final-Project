{
    // Edit these values if your compiler or debugger are not on PATH.
    // Default values are the plain program names so tools on PATH are used.
    // If you use MinGW on Windows you can set these to 
    // "C:\\MinGW\\bin\\gcc.exe" and "C:\\MinGW\\bin\\gdb.exe".
    "chess.compilerPath": "gcc",
    "chess.debuggerPath": "gdb"
}